namespace aula3
{
    public class Obstacles : ICell // ICell is an interface on Maps
    {
        //A classe Obstacle deverá armazenar as informações do obstáculo, que será a posição (x, y) e o tipo. 
        //Cada obstáculo deverá possuir um tipo, que poderá ser Water ou Tree.
        
        //colocando os obstaculos no Array Cell
     //char[("WT", 5, 0),("WT",5, 1), ("WT", 5,2)] Dados_obstaculos_vazios;//tentando adicionar coordenadas no Array - terminar
    //char[] array_obstaculos = new Cell[("WT",5,1),("WT",5,2)]; // WT é o tipo White etc x e y sao as coordenadas dos obstaculos

    //Console.WriteLine(array_obstaculos[2]); como mesmo q imprime o array?

    }
}